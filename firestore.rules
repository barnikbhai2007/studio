rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Allow public listing of users and rooms for discovery and stats
    match /userProfiles/{userId} {
      allow read: if true;
      allow write: if isSignedIn() && request.auth.uid == userId;
    }

    match /gameRooms/{gameRoomId} {
      allow read: if true;
      allow write: if isSignedIn();
      
      match /gameRounds/{roundId} {
        allow read, write: if isSignedIn();
      }

      match /emotes/{emoteId} {
        allow read, write: if isSignedIn();
      }
    }

    match /battleHistories/{battleHistoryId} {
      allow read, write: if isSignedIn();
    }

    match /footballers/{footballerId} {
      allow read: if true;
    }
  }
}